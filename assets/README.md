# Assets

This folder is reserved for optional screenshots or diagrams.

No binary assets are committed by default to keep this mini project small.
If you add a screenshot (for example `screenshot.png`), reference it from
the README like this:

```markdown
![CipherGuard screenshot](assets/screenshot.png)
```
